# Build-time env for `next build` in Cloud Build.
# `.env.local` is gitignored so the production build doesn't see it; values that
# need to be baked into the client bundle (NEXT_PUBLIC_*) live here. These are
# not secrets — they end up in the served JS regardless.

# Privy embedded wallet (email magic-link sign-in)
NEXT_PUBLIC_PRIVY_APP_ID=cmom2ijrw00040cl4hd9qvjnq
