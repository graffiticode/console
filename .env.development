NEXT_PUBLIC_GC_AUTH_URL="http://localhost:4100"
NEXT_PUBLIC_GC_API_URL="http://localhost:3100"

# Form-view app for the "Open in form view" buttons. Run the app on a non-3000
# port locally (e.g. `next dev -p 3001`); prod defaults to https://app.graffiticode.org.
NEXT_PUBLIC_FORM_APP_URL="http://localhost:3001"

GOOGLE_CLOUD_PROJECT="graffiticode"
FIREBASE_AUTH_EMULATOR_HOST="localhost:9099"
FIRESTORE_EMULATOR_HOST="localhost:8080"

NEXT_PUBLIC_PRIVY_APP_ID=""
